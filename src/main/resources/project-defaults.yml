swarm:
  datasources:
    data-sources:
      CitiesDS:
        driver-name: postgresql
        connection-url: jdbc:postgresql://localhost:5432/cities
        user-name: postgres
        password: postgres